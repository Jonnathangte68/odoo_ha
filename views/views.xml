<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="ghda.list">
      <field name="name">ghda list</field>
      <field name="model">ghda.ghda</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="ghda.action_window">
      <field name="name">ghda window</field>
      <field name="res_model">ghda.ghda</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="ghda.action_server">
      <field name="name">ghda server</field>
      <field name="model_id" ref="model_ghda_ghda"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="ghda" id="ghda.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="ghda.menu_1" parent="ghda.menu_root"/>
    <menuitem name="Menu 2" id="ghda.menu_2" parent="ghda.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="ghda.menu_1_list" parent="ghda.menu_1"
              action="ghda.action_window"/>
    <menuitem name="Server to list" id="ghda" parent="ghda.menu_2"
              action="ghda.action_server"/>
    -->
  </data>
</odoo>